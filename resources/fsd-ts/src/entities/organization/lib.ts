import { Organization } from './types';

export function mapOrganization(organization: Organization): Organization {
    return organization;
}

export function mapOrganizations(
    organizations: Array<Organization>,
): Array<Organization> {
    return organizations;
}
